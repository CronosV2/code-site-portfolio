{
  "name": "vortex-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Vortex - Framework web basé sur Next.js avec syntaxe personnalisée",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "@types/node": "^20.10.5",
    "turbo": "^1.11.2"
  },
  "packageManager": "pnpm@8.12.1"
}
