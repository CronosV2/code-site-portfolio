# Vortex

Bienvenue sur un projet Vortex !

- La configuration principale se fait dans `vortex.config.ts`.
- Le fichier `next.config.ts` n'est plus utilisé.
- Créez vos pages dans `src/app/` avec la syntaxe `.vtx`.
